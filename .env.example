# LazyAI Studio Docker 环境变量配置文件
# 复制此文件为 .env 并根据需要修改配置

# ========== 应用环境配置 ==========
# 运行环境类型
# - local: 本地环境（默认，允许所有CORS来源）
# - remote: 远程环境（可配置CORS来源）
ENVIRONMENT=local

# 调试模式 (true/false)
DEBUG=false

# 日志级别 (DEBUG/INFO/WARNING/ERROR)
LOG_LEVEL=INFO

# ========== 服务器配置 ==========
# 绑定主机地址
HOST=0.0.0.0

# 服务端口
PORT=8000

# ========== CORS 配置 ==========
# 允许的来源，多个用逗号分隔
# 示例: http://localhost:3000,https://yourdomain.com
# 注意：local 环境会自动允许所有来源，此配置仅在 remote 环境生效
CORS_ORIGINS=*

# 是否允许携带凭证 (true/false)
# 注意：local 环境会自动设为 true，此配置仅在 remote 环境生效
CORS_ALLOW_CREDENTIALS=true

# ========== 数据库和缓存配置 ==========
# 数据库文件路径
DATABASE_PATH=/app/data/lazyai.db

# 缓存过期时间（秒）
CACHE_TTL=3600

# ========== 远程环境示例配置 ==========
# 如果部署到远程环境，建议使用以下配置：
# ENVIRONMENT=remote
# DEBUG=false
# LOG_LEVEL=INFO
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
# CORS_ALLOW_CREDENTIALS=false

# ========== 本地环境示例配置 ==========
# 本地环境配置（默认）：
# ENVIRONMENT=local
# DEBUG=true
# LOG_LEVEL=DEBUG
# 注意：local 环境自动允许所有 CORS 来源